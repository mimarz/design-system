name: build, test & lint
on:
  workflow_dispatch:
  pull_request:
    branches:
      - develop
  push:
    branches:
      - develop
    paths:
      - 'libraries/core-react/**'
jobs:
  setup:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout, install pnpm & cache 🛎️
        run: |
          REPO="https://${GITHUB_ACTOR}:${{ secrets.GITHUB_TOKEN }}@github.com/${GITHUB_REPOSITORY}.git"
          git clone --filter=blob:none --no-checkout --depth 1 --sparse $REPO .
          git sparse-checkout init --cone
          git sparse-checkout set libraries/core-react libraries/tokens libraries/icons
          git checkout
      - name: Use Node.js ${{ matrix.node-version }}.x
        uses: actions/setup-node@v2
        with:
          node-version: ${{ matrix.node-version }}
          check-latest: true
      - name: Install pnpm
        run: npm i -g pnpm
      - name: Install dependencies
        run: pnpm recursive install
      - name: The github sha is
        run: echo ${{ github.sha }}
      - name: Cache ~/.pnpm-store
        uses: actions/cache@v2
        with:
          path: |
            ./*
            ~/.pnpm-store
          key: ${{ github.sha }}

      # - name: Install pnpm 📦
      #   uses: pnpm/action-setup@v1.2.1
      #   with:
      #     version: latest
      #     run_install: |
      #       - recursive: true
      # - name: Cache files 📄
      #   uses: actions/cache@v2
      #   id: files
      #   with:
      #     path: |
      #       ./*
      #       ~/.npm
      #       ~/setup-pnpm
      #       **/node_modules
      #       ~/.pnpm-store
      #     key: ${{ github.sha }}
  build-core-react:
    name: Build core-react 🔧
    runs-on: ubuntu-latest
    needs: setup
    steps:
      - uses: actions/cache@v2
        with:
          path: |
            ./*
            ~/.pnpm-store
          key: ${{ github.sha }}
      - run: pnpm build:core-react
  # build-tokens:
  #   runs-on: ubuntu-latest
  #   needs: setup
  #   steps:
  #     - name: Build tokens 🔧
  #       run: pnpm build:tokens
  # build-icons:
  #   runs-on: ubuntu-latest
  #   needs: setup
  #   steps:
  #     - name: Build icons 🔧
  #       run: pnpm build:icons
  # test:
  #   name: Run tests ✅
  #   runs-on: ubuntu-latest
  #   needs: [build-core-react, build-icons, build-tokens]
  #   steps:
  #     - run: pnpm --filter ./libraries/core-react run test
  # lint:
  #   runs-on: ubuntu-latest
  #   needs: setup
  #   steps:
  #     - name: Lint core-react and stories ✅
  #       run: pnpm run lint ./libraries/core-react

  # build-storybook:
  #   runs-on: ubuntu-latest
  #   needs: setup
  #   steps:
  #     - name: Build storybook 🔧
  #       run: pnpm build:storybook
